import SharedLayout from 'layouts/SharedLayout';
import type {SharedLayoutProps} from "common/interfaces/layouts";

export default function AccountantLayout(props: SharedLayoutProps) {
  return <SharedLayout {...props} />;
}
