// Increment whenever a saved report layout needs a one-time reset.
// v3 replaces the legacy payout-only layout with the mixed operations layout.
export const CACHE_VERSION = 3;
export const CACHE_PREFIX = "glame-admin-report-state";
